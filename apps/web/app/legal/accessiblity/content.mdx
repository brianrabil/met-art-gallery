import { meta } from "@/lib/meta";

# Accessibility Statement

{meta.siteName} is committed to meeting WCAG 2.1 AA standards. If you encounter an accessibility barrier, email <a href={`mailto:${meta.contact.email}`} className="underline">{meta.contact.email}</a> and we will respond within two business days.
