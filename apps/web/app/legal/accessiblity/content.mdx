import { meta } from "@/lib/meta";
const { siteName, contact } = meta;

# Accessibility Statement

{siteName} is committed to meeting WCAG 2.1 AA standards. If you encounter an accessibility barrier, email <a href={`mailto:${contact.email}`} className="underline">{contact.email}</a> and we will respond within two business days.
