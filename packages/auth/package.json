{
	"name": "@repo/auth",
	"type": "module",
	"private": true,
	"scripts": {
		"auth:secret": "bunx @better-auth/cli@latest secret",
		"auth:generate": "dotenvx run --convention=nextjs -- bunx @better-auth/cli@latest generate"
	},
	"dependencies": {
		"@repo/db": "workspace:*",
		"@repo/utils": "workspace:*",
		"better-auth": "^1.2.7",
		"drizzle-orm": "^0.43.1"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"exports": {
		"./*": "./src/*.ts"
	}
}
